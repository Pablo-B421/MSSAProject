@model IEnumerable<BabyTracker2.Models.DiaperChange>

@{
    ViewData["Title"] = "Index";
}

<h1>DiaperChanges</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BabyInfo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BabyInfo.BabyID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumberofDiaperChanges">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumberofDiaperChanges">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumberofDiaperChanges">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
