@model IEnumerable<BabyTracker.Models.BabyInfo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.BabyGender)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.BirthDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Users)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.BabyGender)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BirthDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Users.UserID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BabyName">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BabyName">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BabyName">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
